{
  "name": "@dept-canvas/frontend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'lint: frontend (no-op)'",
    "test": "vitest run tests/*.spec.ts"
  },
  "dependencies": {
    "@cesdk/cesdk-js": "^1.76.1"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
