{
  "name": "@dept-canvas/renderer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./cesdk-render": "./src/cesdk-render.ts",
    "./worker": "./src/worker.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'lint: renderer (no-op)'",
    "test": "vitest run --config ../vitest.config.ts"
  },
  "dependencies": {
    "@cesdk/node": "1.76.1"
  },
  "devDependencies": {
    "@types/node": "^22.15.30",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
